// ============================================================
// TEST: reserved keyword used as a method name — must FAIL
// Expected error: "reserved keyword"
// ============================================================

struct Counter {
    val: int;
    fn if(self) -> int { return self.val; };  // ERROR: 'if' is a reserved keyword
};
